# Per-service only. Global journald storage policy is deliberately NOT changed.
# The burst is deliberately generous: it bounds a runaway loop without
# suppressing recovery and failure lines during an incident.
[Service]
LogRateLimitIntervalSec=30
LogRateLimitBurst=20000
